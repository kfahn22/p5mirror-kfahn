class Drum {
  contructor() {
    this.x = x;
    this.y = y;
    this.r1 = 30;
    this.r2 = 10;
    this.points
  }
  
  addPoints() {
    for (let theta = 0; theta < 360; theta +=10) {
    let x = this.r1*cos(theta);
    let y = this.r1*sin(theta);
    
    let 
    }
  }
}